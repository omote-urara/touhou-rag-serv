[project]
name = "t-api-v1"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "hashizume", email = "hashizume" }
]
requires-python = ">=3.14"
dependencies = [
    "azure-cosmos>=4.0.0",
    "azure-search-documents>=12.0.0",
    "fastapi>=0.115.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "uvicorn[standard]>=0.30.0",
]

[project.scripts]
t-api-v1 = "t_api_v1:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
